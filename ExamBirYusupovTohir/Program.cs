namespace ExamBirYusupovTohir
{
    internal class Program
    {
        static void Main(string[] args)
        {


        }

        // #1
        //public static bool CheckStringForCrudContains(string text)
        //{
        //    var startsWithCrudCheck = text.StartsWith("crud");
        //    var endsWithCrudCheck = text.EndsWith("crud");

        //    if (startsWithCrudCheck is true && endsWithCrudCheck is true)
        //    {
        //        return true;
        //    }

        //    return false;
        //}

        // #2
        //public static int GetOddElements(List<int> elements)
        //{
        //    var counter = 0;
        //    foreach (var element in elements)
        //    {
        //        if (element % 2 == 0 && (element > 99 && element < 1000))
        //        {
        //            counter++;
        //        }
        //    }

        //    return counter;
        //}

        // #3
        //public static bool ContainsLowerCase(List<string> elements)
        //{
        //    var boolValue = true;

        //    foreach (var element in elements)
        //    {
        //        if (!char.IsLower(element[0]) && !char.IsLower(element[element.Length - 1]))
        //        {
        //            boolValue = false;
        //        }
        //    }

        //    return boolValue;
        //}

        // #4
        //public static string GetMidString(string text)
        //{
        //    var removedText = text.Remove(0, 2);
        //    removedText = removedText.Remove(removedText.Length - 2);

        //    return removedText;
        //}

    }
}

